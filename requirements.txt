# Web app: Flask + SQLAlchemy (SQLite via Flask-SQLAlchemy)
# CLI (app.py) uses the standard library for JSON persistence.

# Flask for web_app.py
flask>=3.0.0
sqlalchemy>=2.0.0
flask-sqlalchemy>=3.1.0
python-dotenv>=1.0.0
gunicorn>=21.2.0
cryptography>=41.0.0
